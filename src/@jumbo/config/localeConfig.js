const localeConfig = {
    locale: 'en',
};

export const localeActionsConfig = {
    switchLocale: () => {
        throw Error("this function is not setup yet in LocaleActionsContext.");
    }
};

export default localeConfig;